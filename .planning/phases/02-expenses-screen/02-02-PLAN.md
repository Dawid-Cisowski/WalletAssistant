---
phase: 02-expenses-screen
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/FilterState.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesViewModel.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/DateRangePickerModal.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/CategoryFilterRow.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/DateQuickChips.kt"
autonomous: false
requirements: [EXP-02, EXP-03, EXP-04]
must_haves:
  truths:
    - "Three quick date chips ('Ten miesiąc' / 'Poprzedni' / 'Niestandardowy') appear above the list; 'Ten miesiąc' is selected on launch"
    - "Tapping 'Poprzedni' reloads expenses for the previous calendar month"
    - "Tapping 'Niestandardowy' opens a Material3 DateRangePicker; confirming reloads expenses for the chosen range and updates the summary card label"
    - "A horizontal LazyRow shows all 13 category FilterChips; tapping chips narrows the visible list client-side; multiple chips can be active"
    - "With no category chip selected, all expenses for the date range are shown"
    - "The summary card total recomputes from the currently displayed (date-ranged + category-filtered) list"
    - "Loading shows a centered spinner; network failure shows the error state with a 'Spróbuj ponownie' retry; an empty result shows the empty state"
  artifacts:
    - path: "app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/FilterState.kt"
      provides: "FilterState data class (dateRange, selectedCategories, activeDateChip) + DateChip enum"
      contains: "data class FilterState"
    - path: "app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/DateRangePickerModal.kt"
      provides: "Material3 DateRangePicker wrapped in DatePickerDialog"
      contains: "DateRangePicker"
    - path: "app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/CategoryFilterRow.kt"
      provides: "LazyRow of 13 category FilterChips"
      contains: "LazyRow"
    - path: "app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/DateQuickChips.kt"
      provides: "Row of 3 date quick FilterChips"
      contains: "FilterChip"
  key_links:
    - from: "expenses/ui/ExpensesScreen.kt"
      to: "expenses/ExpensesViewModel.kt"
      via: "filterState collected; onDateChipSelected / onDateRangeSelected / onCategoryToggled callbacks"
      pattern: "onCategoryToggled"
    - from: "expenses/ui/DateRangePickerModal.kt"
      to: "expenses/ExpensesViewModel.kt"
      via: "onDateRangeConfirmed(start, end) -> viewModel.onDateRangeSelected"
      pattern: "onDateRangeConfirmed"
    - from: "expenses/ExpensesViewModel.kt"
      to: "expenses/FilterState.kt"
      via: "MutableStateFlow<FilterState> seeded from FilterState.default()"
      pattern: "FilterState\\.default"
---

<objective>
Complete the Expenses screen by adding the three remaining user-facing slices on top of Plan 01's working list: date range selection (quick chips + Material3 DateRangePicker — EXP-02), multi-select category filter chips applied client-side (EXP-03), a summary card total that recomputes from the currently displayed list (EXP-04), and the full loading/error/empty states from the UI-SPEC.

Each task here is a usable slice: after the date-range task the user can change the period; after the filter task the user can narrow by category; after the states task every screen condition is handled. Total recomputation (EXP-04) is folded into the date-range and filter tasks because the total is derived from whatever list those filters produce — it is not a separable layer.

Purpose: Finish EXP-02/03/04 and lock the filter-state pattern (two StateFlows, client-side derive) for Phase 3/4 reuse.
Output: A fully interactive read-only Expenses screen handling all filter and UI-state combinations.
</objective>

## Phase Goal

**As a** WalletAssistant user, **I want to** change the date period and filter by category and see the total update for what I'm looking at, **so that** I can answer questions like "how much did I spend on transport last month".

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expenses-screen/02-CONTEXT.md
@.planning/phases/02-expenses-screen/02-RESEARCH.md
@.planning/phases/02-expenses-screen/02-PATTERNS.md
@.planning/phases/02-expenses-screen/02-UI-SPEC.md
@.planning/phases/02-expenses-screen/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add FilterState + date range selection (quick chips and DateRangePicker) recomputing the total</name>
  <files>
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/FilterState.kt,
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesViewModel.kt,
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/DateQuickChips.kt,
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/DateRangePickerModal.kt,
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt
  </files>
  <read_first>
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesViewModel.kt (Plan 01 ViewModel — extend, do not rewrite; preserve sort + total logic)
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt (Plan 01 screen — add chips + picker above the existing list/summary)
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesUiState.kt (Success carries the displayed list + total)
    - .planning/phases/02-expenses-screen/02-RESEARCH.md (Pattern 2 FilterState, Pattern 5 DateRangePicker in DatePickerDialog, Pitfall 6 @OptIn, Pitfall 7 Warsaw timezone)
    - .planning/phases/02-expenses-screen/02-PATTERNS.md (FilterState.kt + DateChip code; @OptIn guard shared pattern; Warsaw timezone shared pattern)
    - .planning/phases/02-expenses-screen/02-UI-SPEC.md (Date Quick Chips, Summary Card period labels, Date picker copywriting, Interaction Contract date rows)
    - .planning/phases/02-expenses-screen/02-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <action>
    Create expenses/FilterState.kt: `data class FilterState(val dateRange: Pair<LocalDate, LocalDate>, val selectedCategories: Set<ExpenseCategory>, val activeDateChip: DateChip)` with a companion `default()` computing today = LocalDate.now(ZoneId.of("Europe/Warsaw")), dateRange = today.withDayOfMonth(1) to today, selectedCategories = emptySet(), activeDateChip = DateChip.THIS_MONTH. Add `enum class DateChip { THIS_MONTH, PREVIOUS_MONTH, CUSTOM }`. (D-06, Pitfall 7)

    Extend ExpensesViewModel: add a private MutableStateFlow<FilterState> seeded from FilterState.default() exposed as `val filterState: StateFlow<FilterState>`. Refactor the Plan 01 init to seed the range from filterState rather than a local computation (keep the same Warsaw-month default). Add `fun onDateChipSelected(chip: DateChip)`: for THIS_MONTH set range first-of-this-month → today; for PREVIOUS_MONTH set range first → last day of the previous calendar month (compute with today.minusMonths(1), withDayOfMonth(1) and the month's lengthOfMonth, all in Warsaw zone); update activeDateChip; trigger a reload for the new range. CUSTOM is not selected directly by this method — it becomes active only via onDateRangeSelected. Add `fun onDateRangeSelected(from: LocalDate, to: LocalDate)`: update dateRange, set activeDateChip = CUSTOM, reload for that range. The reload path must continue to sort newest-first and recompute total inside the ViewModel (EXP-04 — total always derives from the current Success list). retry() reloads the current filterState.dateRange.

    Create expenses/ui/DateQuickChips.kt: a `Row(horizontalArrangement = Arrangement.spacedBy(8.dp), modifier = padding(horizontal = 16.dp))` of three `FilterChip`s with exact labels "Ten miesiąc", "Poprzedni", "Niestandardowy" (UI-SPEC Copywriting). selected reflects activeDateChip (THIS_MONTH/PREVIOUS_MONTH/CUSTOM); onClick for the first two calls onDateChipSelected, the third opens the date picker. Touch targets respect the 48dp minimum.

    Create expenses/ui/DateRangePickerModal.kt per RESEARCH.md Pattern 5: `@OptIn(ExperimentalMaterial3Api::class) @Composable fun DateRangePickerModal(onDateRangeConfirmed: (LocalDate, LocalDate) -> Unit, onDismiss: () -> Unit)` using rememberDateRangePickerState inside a DatePickerDialog. Confirm button labelled "Zastosuj" is enabled only when both selectedStartDateMillis and selectedEndDateMillis are non-null (Pitfall 6 null guard); on confirm convert each epoch-millis via Instant.ofEpochMilli(millis).atZone(ZoneId.of("Europe/Warsaw")).toLocalDate() (Pitfall 7) and call onDateRangeConfirmed then onDismiss. Dismiss button labelled "Anuluj". DateRangePicker title "Wybierz zakres dat", showModeToggle = false.

    Update ExpensesScreen.kt: collect filterState via collectAsStateWithLifecycle alongside uiState. Place DateQuickChips above the existing list (per UI-SPEC layout the order is Summary Card → Date Quick Chips → [Category Filter Row in Task 2] → Expense List). Hold a `var showPicker by remember { mutableStateOf(false) }`; "Niestandardowy" sets it true; when true render DateRangePickerModal wiring onDateRangeConfirmed → viewModel.onDateRangeSelected and onDismiss → showPicker = false (cancel restores previous selection by simply not changing state). Update the summary card period label to reflect the active range: a month-name + year label ("Czerwiec 2026") when the range is a full calendar month, otherwise the custom-range format "d MMM — d MMM yyyy" ("1 cze — 18 cze 2026") per D-05 / UI-SPEC.
  </action>
  <verify>
    <automated>cd ~/AndroidStudioProjects/WalletAssistantAndroid && ./gradlew :app:assembleDebug --console=plain 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` completes with BUILD SUCCESSFUL
    - FilterState.kt declares FilterState(dateRange, selectedCategories, activeDateChip) and `enum class DateChip { THIS_MONTH, PREVIOUS_MONTH, CUSTOM }`; default() uses ZoneId.of("Europe/Warsaw") and withDayOfMonth(1)
    - ExpensesViewModel exposes `val filterState: StateFlow<FilterState>` and functions onDateChipSelected(DateChip) and onDateRangeSelected(LocalDate, LocalDate); total is still computed in the ViewModel reload path (grep shows sumOf in the ViewModel, not in any ui/ file)
    - DateQuickChips.kt contains the exact strings "Ten miesiąc", "Poprzedni", "Niestandardowy"
    - DateRangePickerModal.kt carries `@OptIn(ExperimentalMaterial3Api::class)`, uses rememberDateRangePickerState, gates the "Zastosuj" button on both start and end millis being non-null, and converts millis via ZoneId.of("Europe/Warsaw"); contains "Anuluj" and "Wybierz zakres dat"
    - ExpensesScreen.kt collects filterState via collectAsStateWithLifecycle and renders DateQuickChips above the expense list
  </acceptance_criteria>
  <done>The user can switch between this month, previous month, and a custom DateRangePicker range; the list and the summary-card total update for the chosen period (EXP-02, partial EXP-04).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the 13-category multi-select FilterChip row with client-side filtering and total recompute</name>
  <files>
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesViewModel.kt,
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/CategoryFilterRow.kt,
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt
  </files>
  <read_first>
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesViewModel.kt (Task 1 state — add category filtering to the derive path)
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpenseCategory.kt (13 values + displayName + fromString from Plan 01)
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt (insert CategoryFilterRow between DateQuickChips and the list)
    - .planning/phases/02-expenses-screen/02-RESEARCH.md (Pattern 2 onCategoryToggled; "Filtering in Composable" + "Calculating total in Composable" anti-patterns)
    - .planning/phases/02-expenses-screen/02-UI-SPEC.md (Category Filter Row spec; category chip label table; Interaction Contract category rows)
    - .planning/phases/02-expenses-screen/02-CONTEXT.md (D-07, D-08, D-09, D-10, D-14)
  </read_first>
  <action>
    Extend ExpensesViewModel: add `fun onCategoryToggled(category: ExpenseCategory)` that toggles the category in filterState.selectedCategories (remove if present, add if absent) without a network call (D-09 — category filtering is purely client-side). The displayed list and total must derive from: the raw loaded list for the current date range, narrowed to selectedCategories (empty set = no narrowing, show all — D-09), sorted newest-first, with total = sum of the narrowed amounts. Implement this so a category toggle re-emits ExpensesUiState.Success(narrowedSortedList, recomputedTotal) without re-fetching — e.g. keep the raw fetched list in a private field or combine the network list with filterState and map to Success. Filtering, sorting, and totalling all stay in the ViewModel (D-14; do not move any of these into a Composable).

    Create expenses/ui/CategoryFilterRow.kt per UI-SPEC: `LazyRow(contentPadding = PaddingValues(horizontal = 16.dp), horizontalArrangement = Arrangement.spacedBy(8.dp))` rendering one FilterChip per ExpenseCategory.entries (all 13 including SAVINGS_TRANSFER — D-07), label = category.displayName (D-10 Polish labels), selected = category in selectedCategories, onClick = { onCategoryToggled(category) }. Initial state shows no chip selected. Multiple chips can be active simultaneously (EXP-03). Touch targets respect the 48dp minimum.

    Update ExpensesScreen.kt: insert CategoryFilterRow between DateQuickChips and the expense list (UI-SPEC layout order). Pass filterState.selectedCategories and viewModel::onCategoryToggled. The summary card total already reads from ExpensesUiState.Success.total, which now reflects the category-narrowed list — no total math in the Composable.
  </action>
  <verify>
    <automated>cd ~/AndroidStudioProjects/WalletAssistantAndroid && ./gradlew :app:assembleDebug --console=plain 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` completes with BUILD SUCCESSFUL
    - ExpensesViewModel has `fun onCategoryToggled(ExpenseCategory)` that does not call the repository (no repository.getExpenses inside it) — toggling re-derives from the already-loaded list
    - The derive path narrows to selectedCategories only when the set is non-empty and recomputes total over the narrowed list inside the ViewModel
    - CategoryFilterRow.kt uses LazyRow over ExpenseCategory.entries (13 chips) with FilterChip selected = `category in selectedCategories` and labels from category.displayName
    - grep -rn 'filter\|sumOf\|sortedBy\|sortedWith' app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui returns no list-filtering/totalling logic (all in the ViewModel)
    - ExpensesScreen renders CategoryFilterRow between the date chips and the expense list
  </acceptance_criteria>
  <done>The user can tap any of 13 category chips (multi-select) to narrow the visible expenses client-side, and the summary total recomputes for the narrowed list (EXP-03, EXP-04 complete).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement full loading / error / empty UI states per UI-SPEC</name>
  <files>
    ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt
  </files>
  <read_first>
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ui/ExpensesScreen.kt (current branch handling from Plan 01 Task 3 + this plan's tasks — replace the minimal Loading/Error with full states, add empty state)
    - app/src/main/kotlin/com/dawidcisowski/walletassistant/expenses/ExpensesViewModel.kt (retry() and ExpensesUiState branches)
    - .planning/phases/02-expenses-screen/02-UI-SPEC.md (Loading State, Error State, Empty State component specs; Copywriting contract; Color roles error/primary; icon names ErrorOutline / ReceiptLong)
    - .planning/phases/02-expenses-screen/02-CONTEXT.md (D-14 — empty result still shows the date chips/filters, only the list area is empty)
  </read_first>
  <action>
    Update ExpensesScreen.kt state rendering to match UI-SPEC exactly:

    Loading: a full-area `Box(contentAlignment = Alignment.Center) { CircularProgressIndicator(color = MaterialTheme.colorScheme.primary) }` shown for ExpensesUiState.Loading (no text — Copywriting contract). The date chips and category row need not render during the initial Loading.

    Error: for ExpensesUiState.Error render a centered `Column(horizontalAlignment = CenterHorizontally, verticalArrangement = Center)` with `Icon(Icons.Default.ErrorOutline, tint = colorScheme.error)` sized 48dp, `Text("Nie udało się wczytać wydatków", style = titleLarge, textAlign = Center)`, `Text("Sprawdź połączenie z internetem i spróbuj ponownie.", style = bodyMedium, textAlign = Center)`, and `Button(onClick = { viewModel.retry() }) { Text("Spróbuj ponownie") }`. Use the exact Polish strings from the Copywriting contract.

    Empty: for ExpensesUiState.Success whose displayed list is empty, render the list area as a centered `Column` with `Icon(Icons.Default.ReceiptLong, tint = colorScheme.primary)` sized 64dp, `Text("Brak wydatków", style = titleLarge, textAlign = Center)`, `Text("Nie znaleziono wydatków dla wybranego okresu i filtrów.", style = bodyMedium, textAlign = Center)`. The summary card, date quick chips, and category filter row remain visible above the empty-state block (D-14 — the user can still change filters), only the list region shows the empty illustration.

    Use only the icons named in UI-SPEC (Icons.Default.ErrorOutline, Icons.Default.ReceiptLong — provided by material-icons-extended), the declared typography slots (titleLarge, bodyMedium), and the declared color roles (error, primary). Do not introduce new strings, sizes, or colors beyond UI-SPEC.
  </action>
  <verify>
    <automated>cd ~/AndroidStudioProjects/WalletAssistantAndroid && ./gradlew :app:assembleDebug --console=plain 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:assembleDebug` completes with BUILD SUCCESSFUL
    - ExpensesScreen.kt renders CircularProgressIndicator for Loading, the error block (ErrorOutline icon + both error strings + "Spróbuj ponownie" Button wired to viewModel.retry()) for Error, and the empty block (ReceiptLong icon + "Brak wydatków" + empty body string) for an empty Success list
    - The exact strings "Nie udało się wczytać wydatków", "Sprawdź połączenie z internetem i spróbuj ponownie.", "Spróbuj ponownie", "Brak wydatków", and "Nie znaleziono wydatków dla wybranego okresu i filtrów." appear in ExpensesScreen.kt
    - In the empty-Success case the summary card and the date/category filter rows still render (the empty illustration replaces only the list region)
    - Icons referenced are Icons.Default.ErrorOutline and Icons.Default.ReceiptLong
  </acceptance_criteria>
  <done>Every screen condition — loading, network error with retry, and empty result — renders per the UI-SPEC, completing the read-only Expenses screen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify date range, category filtering, total, and all UI states</name>
  <action>Pause for human verification. Follow the steps in <how-to-verify> below and wait for the resume signal before proceeding.</action>
  <what-built>The Expenses screen is now fully interactive: three quick date chips plus a Material3 DateRangePicker (EXP-02), a 13-category multi-select FilterChip row applying client-side filtering (EXP-03), a summary card total that recomputes from the displayed list (EXP-04), and full loading/error/empty states per the UI-SPEC.</what-built>
  <how-to-verify>
    1. With the backend running and reachable, run `./gradlew installDebug` (or run from Android Studio) and launch the app.
    2. Date range: confirm "Ten miesiąc" is selected on launch. Tap "Poprzedni" — the list reloads for last calendar month and the card label/total update. Tap "Niestandardowy" — a date picker titled "Wybierz zakres dat" opens; pick a start and end, tap "Zastosuj" — the list reloads for that range and the card label shows "d MMM — d MMM yyyy"; tap it again and "Anuluj" leaves the selection unchanged.
    3. Category filter: confirm 13 chips are visible in a single scrollable row with Polish labels (including "Transfer na oszczędności"). With none selected, all range expenses show. Tap one chip — the list narrows and the total drops to the sum of that category. Tap a second chip — both categories show (multi-select). Deselect all — full list returns.
    4. Total correctness: pick a category and confirm the card total equals the sum of the visible rows.
    5. States: turn off the backend (or airplane mode) and tap "Spróbuj ponownie" / relaunch — confirm the error state (icon + "Nie udało się wczytać wydatków" + retry button). Pick a date range or category combination with no expenses — confirm the empty state ("Brak wydatków") while the filter chips remain usable above it. Confirm the loading spinner appears briefly on each reload.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what behaved incorrectly (wrong total after filtering, picker timezone off by a day, missing category, state not showing, multi-select not working).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User date input → ViewModel → backend query | DateRangePicker millis are converted to LocalDate and sent as from/to query params |
| Backend expense data → Composable render | Filtered expense fields are displayed on screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Input Validation (V5) | DateRangePicker confirm | mitigate | "Zastosuj" is disabled until both start and end millis are non-null; ViewModel only issues a query with a valid LocalDate pair — no malformed range reaches the backend |
| T-02-07 | Tampering / correctness | Epoch-millis → LocalDate conversion | mitigate | All millis→LocalDate and now() conversions use ZoneId.of("Europe/Warsaw") (matches backend timezone), preventing off-by-one-day range errors (Pitfall 7) |
| T-02-08 | Information Disclosure | ExpenseDto fields in filter/render path | mitigate | No Log.* of ExpenseDto contents introduced; filtering and totalling stay in-memory in the ViewModel |
| T-02-09 | Denial of Service (self) | Client-side category filter over loaded list | accept | Personal-use, no pagination, single-month dataset — re-filtering the in-memory list is negligible cost; no rate limit needed |
</threat_model>

<verification>
- `./gradlew :app:assembleDebug` BUILD SUCCESSFUL across all three tasks
- EXP-02: quick chips + DateRangePicker change the loaded range; Warsaw-zone conversion prevents off-by-one
- EXP-03: 13-chip LazyRow, multi-select, client-side narrowing; empty set = show all
- EXP-04: summary total always equals the sum of the currently displayed (ranged + category-filtered) list, computed in the ViewModel
- Loading/Error/Empty states render exactly per UI-SPEC strings, icons, and colors
- No filtering/sorting/totalling logic in any expenses/ui file (all in the ViewModel — D-09, D-14)
</verification>

<success_criteria>
- EXP-02, EXP-03, EXP-04 satisfied: date range selection, multi-select category filtering, and a live-recomputed total card all work on the read-only Expenses screen
- All UI states (loading/error/empty) handled per UI-SPEC, establishing the pattern Phase 4 reuses for Accounts/Investments
- Filter-state architecture (StateFlow<FilterState> + client-side derive) locked for Phase 3 reuse
</success_criteria>

<output>
Create `.planning/phases/02-expenses-screen/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02)

New files:
- `expenses/FilterState.kt` — `data class FilterState(dateRange: Pair<LocalDate, LocalDate>, selectedCategories: Set<ExpenseCategory>, activeDateChip: DateChip)` with `companion object { fun default(): FilterState }`; `enum class DateChip { THIS_MONTH, PREVIOUS_MONTH, CUSTOM }`
- `expenses/ui/DateQuickChips.kt` — `@Composable fun DateQuickChips(...)` (3 FilterChips)
- `expenses/ui/DateRangePickerModal.kt` — `@OptIn(ExperimentalMaterial3Api::class) @Composable fun DateRangePickerModal(onDateRangeConfirmed: (LocalDate, LocalDate) -> Unit, onDismiss: () -> Unit)`
- `expenses/ui/CategoryFilterRow.kt` — `@Composable fun CategoryFilterRow(...)` (LazyRow of 13 FilterChips)

Modified files:
- `expenses/ExpensesViewModel.kt` — adds `val filterState: StateFlow<FilterState>`, `fun onDateChipSelected(DateChip)`, `fun onDateRangeSelected(LocalDate, LocalDate)`, `fun onCategoryToggled(ExpenseCategory)`
- `expenses/ui/ExpensesScreen.kt` — adds date chips, category row, date picker wiring, and full loading/error/empty states
