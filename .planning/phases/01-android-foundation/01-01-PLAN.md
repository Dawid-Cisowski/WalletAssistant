---
phase: 01-android-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/AndroidStudioProjects/WalletAssistantAndroid/settings.gradle.kts"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/build.gradle.kts"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/gradle/wrapper/gradle-wrapper.properties"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/.gitignore"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/local.properties.example"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/build.gradle.kts"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/AndroidManifest.xml"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/WalletAssistantApp.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/MainActivity.kt"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/res/layout/activity_main.xml"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/res/values/strings.xml"
  - "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/res/values/themes.xml"
autonomous: true
requirements: [AUTH-02]
user_setup:
  - service: local.properties
    why: "Build-time secret injection — credentials must never be committed (AUTH-02, D-05, D-06)"
    env_vars:
      - name: backend_url
        source: "local.properties — emulator default http://10.0.2.2:8080, physical device uses LAN IP (e.g. http://192.168.x.x:8080)"
      - name: hmac_secret
        source: "Generate with: openssl rand -base64 32 — same value must be registered on backend HMAC_DEVICES_JSON in Plan 02"
      - name: device_id
        source: "Static string, e.g. wallet-android-device — registered as a NEW entry in backend HMAC_DEVICES_JSON"

must_haves:
  truths:
    - "The Android project builds successfully with ./gradlew assembleDebug"
    - "BuildConfig exposes BACKEND_URL, HMAC_SECRET, and DEVICE_ID fields read from local.properties"
    - "local.properties is git-ignored from the very first commit — git never tracks it"
    - "The app launches MainActivity with the INTERNET permission and cleartext HTTP allowed for local dev"
  artifacts:
    - path: "~/AndroidStudioProjects/WalletAssistantAndroid/app/build.gradle.kts"
      provides: "buildConfigField injection of three credentials from local.properties"
      contains: "buildConfigField"
    - path: "~/AndroidStudioProjects/WalletAssistantAndroid/.gitignore"
      provides: "local.properties exclusion from first commit"
      contains: "local.properties"
    - path: "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/AndroidManifest.xml"
      provides: "INTERNET permission + cleartext traffic flag + MainActivity launcher"
      contains: "android.permission.INTERNET"
    - path: "~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/MainActivity.kt"
      provides: "Launcher activity (verification call wired in Plan 02)"
      contains: "class MainActivity"
  key_links:
    - from: "app/build.gradle.kts"
      to: "local.properties"
      via: "java.util.Properties loads rootProject.file(local.properties), injected via buildConfigField"
      pattern: "rootProject.file\\(\"local.properties\"\\)"
---

<objective>
Scaffold the new WalletAssistant Android project (Kotlin + Kotlin DSL) at `~/AndroidStudioProjects/WalletAssistantAndroid/` with the three networking credentials injected into `BuildConfig` from `local.properties`, and `local.properties` git-ignored from the first commit. This delivers the buildable half of the Walking Skeleton: a launchable app with auth credentials available at runtime, ready for the HMAC interceptor and verification call in Plan 02.

Purpose: Satisfies AUTH-02 (credentials via BuildConfig from local.properties, never in git) and establishes the project structure all subsequent phases build on.
Output: A compilable Android project (assembleDebug succeeds) with `BuildConfig.BACKEND_URL`, `BuildConfig.HMAC_SECRET`, `BuildConfig.DEVICE_ID`, INTERNET permission, cleartext HTTP enabled, and a clean first git commit that excludes secrets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-android-foundation/01-CONTEXT.md
@.planning/phases/01-android-foundation/01-RESEARCH.md
@.planning/phases/01-android-foundation/01-PATTERNS.md
@.planning/phases/01-android-foundation/SKELETON.md

# Reference implementation for SDK versions, dependency versions, buildConfigField syntax:
@/Users/dawidcidowski/AndroidStudioProjects/HealthAssistant/app/build.gradle
@/Users/dawidcidowski/AndroidStudioProjects/HealthAssistant/gradle/wrapper/gradle-wrapper.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project skeleton and Gradle build with secret injection</name>
  <files>~/AndroidStudioProjects/WalletAssistantAndroid/settings.gradle.kts, ~/AndroidStudioProjects/WalletAssistantAndroid/build.gradle.kts, ~/AndroidStudioProjects/WalletAssistantAndroid/gradle/wrapper/gradle-wrapper.properties, ~/AndroidStudioProjects/WalletAssistantAndroid/app/build.gradle.kts</files>
  <read_first>
    - /Users/dawidcidowski/AndroidStudioProjects/HealthAssistant/app/build.gradle (SDK versions, dependency versions, compileOptions, buildConfigField syntax in Groovy — adapt to Kotlin DSL)
    - /Users/dawidcidowski/AndroidStudioProjects/HealthAssistant/gradle/wrapper/gradle-wrapper.properties (Gradle 8.13 distribution URL)
    - .planning/phases/01-android-foundation/01-PATTERNS.md (section "app/build.gradle.kts" — exact Kotlin DSL local.properties injection block, lines 190-271)
    - .planning/phases/01-android-foundation/01-RESEARCH.md (Pattern 1: BuildConfig Secret Injection; Standard Stack table for exact versions)
  </read_first>
  <action>
    Create a single-module Android project at `~/AndroidStudioProjects/WalletAssistantAndroid/`. Use Kotlin DSL build scripts (`.kts`) per D-03.

    Root `settings.gradle.kts`: declare `pluginManagement` and `dependencyResolutionManagement` with `google()`, `mavenCentral()`, `gradlePluginPortal()` repositories; set `rootProject.name = "WalletAssistantAndroid"`; `include(":app")`.

    Root `build.gradle.kts`: declare the Android application plugin and Kotlin Android plugin with `apply false`. Use Android Gradle Plugin 8.13.0 and Kotlin Android 1.9.20 (versions per RESEARCH.md Standard Stack, taken from HealthAssistant).

    `gradle/wrapper/gradle-wrapper.properties`: set `distributionUrl` to Gradle 8.13 (`gradle-8.13-bin.zip`), matching HealthAssistant.

    `app/build.gradle.kts`: apply `com.android.application` and `org.jetbrains.kotlin.android` plugins. Set `namespace = "com.dawidcisowski.walletassistant"` and `applicationId = "com.dawidcisowski.walletassistant"` (D-01). Set `compileSdk = 35`, `minSdk = 26`, `targetSdk = 35`, `versionCode = 1`, `versionName = "1.0"`. Set `compileOptions` sourceCompatibility/targetCompatibility to `JavaVersion.VERSION_11` and `kotlinOptions.jvmTarget = "11"`. Enable `buildFeatures { buildConfig = true }`.

    Implement the local.properties read block at the top of `app/build.gradle.kts`: import `java.util.Properties`, create a `Properties` instance, load from `rootProject.file("local.properties")` only if it exists. Inject all three credentials via `buildConfigField` in BOTH the `debug` AND `release` build types (per D-04 — no product flavors, build types only): `BACKEND_URL`, `HMAC_SECRET`, `DEVICE_ID`, each reading `localProps["backend_url"|"hmac_secret"|"device_id"] ?: ""` wrapped in escaped quotes. Set `release { isMinifyEnabled = false }`.

    Declare dependencies exactly per RESEARCH.md Standard Stack: kotlin-stdlib 1.9.20, kotlinx-coroutines-android 1.7.3, retrofit 2.9.0, converter-gson 2.9.0, okhttp 4.12.0, logging-interceptor 4.12.0, gson 2.10.1, androidx.appcompat:appcompat:1.7.0, androidx.lifecycle:lifecycle-runtime-ktx:2.8.0. Add testImplementation junit:junit:4.13.2.

    Do NOT hardcode any secret value in build scripts — all three credentials come exclusively from local.properties (anti-pattern per RESEARCH.md). Do NOT add product flavors (D-04).
  </action>
  <verify>
    <automated>cd ~/AndroidStudioProjects/WalletAssistantAndroid && grep -q 'rootProject.file("local.properties")' app/build.gradle.kts && grep -c 'buildConfigField' app/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - `app/build.gradle.kts` contains `namespace = "com.dawidcisowski.walletassistant"` and `applicationId = "com.dawidcisowski.walletassistant"`
    - `app/build.gradle.kts` contains `compileSdk = 35`, `minSdk = 26`, `targetSdk = 35`
    - `app/build.gradle.kts` contains `buildConfig = true`
    - `grep -c 'buildConfigField' app/build.gradle.kts` returns at least 6 (3 credentials × debug + release)
    - `app/build.gradle.kts` loads from `rootProject.file("local.properties")` and references `localProps["backend_url"]`, `localProps["hmac_secret"]`, `localProps["device_id"]`
    - No literal base64 secret or backend IP appears in any `.kts` file
    - `gradle-wrapper.properties` references `gradle-8.13-bin.zip`
  </acceptance_criteria>
  <done>Project scaffold exists with Kotlin DSL build scripts; all three credentials injected via buildConfigField from local.properties in both build types; no hardcoded secrets; dependency versions match RESEARCH.md Standard Stack.</done>
</task>

<task type="auto">
  <name>Task 2: App entrypoint, manifest, resources, and secret-safe git baseline</name>
  <files>~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/AndroidManifest.xml, ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/WalletAssistantApp.kt, ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/kotlin/com/dawidcisowski/walletassistant/MainActivity.kt, ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/res/layout/activity_main.xml, ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/res/values/strings.xml, ~/AndroidStudioProjects/WalletAssistantAndroid/app/src/main/res/values/themes.xml, ~/AndroidStudioProjects/WalletAssistantAndroid/.gitignore, ~/AndroidStudioProjects/WalletAssistantAndroid/local.properties.example</files>
  <read_first>
    - .planning/phases/01-android-foundation/01-PATTERNS.md (sections "AndroidManifest.xml", "WalletAssistantApp.kt", "MainActivity.kt" — exact manifest elements and Application class boilerplate)
    - .planning/phases/01-android-foundation/01-RESEARCH.md (Pitfall 6: Cleartext HTTP blocked on Android 9+; Pitfall 7: local.properties already tracked)
    - .planning/phases/01-android-foundation/01-CONTEXT.md (D-06: local.properties in .gitignore from first commit)
  </read_first>
  <action>
    Create `AndroidManifest.xml` under `app/src/main/`: declare `<uses-permission android:name="android.permission.INTERNET" />`. The `<application>` element sets `android:name=".WalletAssistantApp"`, `android:usesCleartextTraffic="true"` (required for local HTTP `10.0.2.2:8080` per RESEARCH.md Pitfall 6 — development only), `android:label="@string/app_name"`, `android:theme="@style/Theme.WalletAssistant"`, `android:allowBackup="true"`. Declare `<activity android:name=".MainActivity" android:exported="true">` with an intent-filter for `android.intent.action.MAIN` + `android.intent.category.LAUNCHER`.

    Create `WalletAssistantApp.kt` (Kotlin, package `com.dawidcisowski.walletassistant`): an empty `Application` subclass with no Phase 1 initialization (a comment may note Phase 2+ will add DI/logging — but do not write the literal class-name token the manifest greps for inside a comment in a way that would duplicate a negative gate; this plan uses only positive greps so plain comments are fine).

    Create `MainActivity.kt` (Kotlin, package `com.dawidcisowski.walletassistant`): an `AppCompatActivity` subclass whose `onCreate` calls `super.onCreate`, then `setContentView(R.layout.activity_main)`. Leave a clearly marked placeholder comment where Plan 02 will wire the HMAC verification coroutine — do NOT implement the network call here (that is Plan 02's vertical slice).

    Create `res/layout/activity_main.xml`: a minimal layout with a single `TextView` showing the app name (this is a no-UI foundation phase per CONTEXT.md; the TextView is placeholder only).

    Create `res/values/strings.xml` with `<string name="app_name">WalletAssistant</string>`.

    Create `res/values/themes.xml` defining `Theme.WalletAssistant` extending a Material/AppCompat parent theme (e.g. `Theme.AppCompat.DayNight.NoActionBar`).

    Create `.gitignore` at project root listing at minimum: `local.properties`, `.gradle/`, `build/`, `.idea/`, `*.iml`, `.DS_Store`, `app/build/`. The `local.properties` entry is mandatory and must be present before the first commit (D-06, RESEARCH.md Pitfall 7).

    Create `local.properties.example` (this file IS committed) documenting the three required keys with placeholder values: `sdk.dir=<path-to-android-sdk>`, `backend_url=http://10.0.2.2:8080`, `hmac_secret=<base64-secret-from-openssl-rand-base64-32>`, `device_id=wallet-android-device`. Use only placeholder values — never a real secret.

    Initialize git in the project directory if not already a repo, and ensure that when the first commit is made, `git status` does not show `local.properties` as tracked. If Android Studio already created `local.properties` with `sdk.dir`, the `.gitignore` entry prevents it from being staged; verify with `git status --porcelain`.
  </action>
  <verify>
    <automated>cd ~/AndroidStudioProjects/WalletAssistantAndroid && grep -q 'android.permission.INTERNET' app/src/main/AndroidManifest.xml && grep -q 'usesCleartextTraffic="true"' app/src/main/AndroidManifest.xml && grep -qx 'local.properties' .gitignore && (git ls-files --error-unmatch local.properties 2>/dev/null && echo "TRACKED-FAIL" || echo "UNTRACKED-OK")</automated>
  </verify>
  <acceptance_criteria>
    - `AndroidManifest.xml` contains `android.permission.INTERNET`
    - `AndroidManifest.xml` contains `android:usesCleartextTraffic="true"`
    - `AndroidManifest.xml` declares `.MainActivity` as launcher (MAIN/LAUNCHER intent-filter) and `android:name=".WalletAssistantApp"` on application
    - `.gitignore` contains a line exactly matching `local.properties`
    - `git ls-files local.properties` returns empty (file is NOT tracked) — the automated check prints `UNTRACKED-OK`
    - `local.properties.example` exists and contains placeholder keys `backend_url`, `hmac_secret`, `device_id` with NO real secret value
    - `MainActivity.kt` defines `class MainActivity` and contains a placeholder comment for the Plan 02 verification call (no network code present)
    - `WalletAssistantApp.kt` defines an `Application` subclass
  </acceptance_criteria>
  <done>App entrypoint, manifest with INTERNET + cleartext, minimal resources, and a secret-safe git baseline all exist; local.properties is git-ignored and untracked from the first commit; MainActivity is a launchable stub awaiting Plan 02's verification call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → APK | local.properties secrets are compiled into BuildConfig (the APK binary) |
| dev machine → git | local.properties must never cross into version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | local.properties (hmac_secret, device_id) | mitigate | `local.properties` in `.gitignore` from first commit (AUTH-02, D-06); verified by `git ls-files local.properties` returning empty in Task 2 |
| T-01-02 | Information Disclosure | BuildConfig fields compiled into APK | accept | Personal-use app, locally-installed APK, single device. ASVS L1 — for distribution Android Keystore would be required, but APK is not distributed (out of scope per REQUIREMENTS.md) |
| T-01-03 | Tampering | Gradle dependency installs (Maven Central / Google Maven) | accept | All dependencies are from `google()` + `mavenCentral()`, versions verified against HealthAssistant production codebase (RESEARCH.md Package Legitimacy Audit — npm gate N/A for Android/Maven) |
</threat_model>

<verification>
- `./gradlew assembleDebug` succeeds once `local.properties` is populated by the developer (build proves BuildConfig fields resolve).
- `git ls-files local.properties` returns empty — secret file never tracked.
- BuildConfig.java generated under `app/build/generated/` contains BACKEND_URL, HMAC_SECRET, DEVICE_ID fields after a build.
</verification>

<success_criteria>
- Project builds (`assembleDebug`) with populated local.properties.
- AUTH-02 partially satisfied: credentials flow from local.properties → BuildConfig; local.properties git-ignored from first commit.
- No secret value appears in any committed file.
- App is launchable (MainActivity stub) and ready for Plan 02's HMAC interceptor + verification call.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files:
- `settings.gradle.kts` — multi-project settings, includes `:app`
- `build.gradle.kts` (root) — plugin declarations (AGP 8.13.0, Kotlin 1.9.20)
- `gradle/wrapper/gradle-wrapper.properties` — Gradle 8.13
- `app/build.gradle.kts` — Android app config, buildConfigField secret injection
- `.gitignore` — excludes local.properties from first commit
- `local.properties.example` — documented placeholder template (committed)
- `app/src/main/AndroidManifest.xml` — INTERNET permission, cleartext flag, MainActivity launcher
- `app/src/main/kotlin/com/dawidcisowski/walletassistant/WalletAssistantApp.kt` — `WalletAssistantApp` (Application subclass)
- `app/src/main/kotlin/com/dawidcisowski/walletassistant/MainActivity.kt` — `MainActivity` (AppCompatActivity, stub)
- `app/src/main/res/layout/activity_main.xml` — placeholder layout
- `app/src/main/res/values/strings.xml` — `app_name`
- `app/src/main/res/values/themes.xml` — `Theme.WalletAssistant`

Generated symbols (build output, consumed by Plan 02):
- `BuildConfig.BACKEND_URL`, `BuildConfig.HMAC_SECRET`, `BuildConfig.DEVICE_ID`
</artifacts_produced>

<output>
Create `.planning/phases/01-android-foundation/01-01-SUMMARY.md` when done
</output>
